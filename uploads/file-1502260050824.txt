Useful Vagrant Commands:
- vagrant init: Initializes vagrant with a Vagrantfile
- vagrant up: Starts the vagrant machine
- vagrant up --provision: Starts the machine with the provision
- vagrant ssh: Starts a ssh session
- vagrant halt: Stops the machine
- vagrant destroy: Stops and deletes the machine
- vagrant box add: Adds a vagrant box image on your computer
- vagrant box list: List of all the vagrant boxes on your computer

rails:

rails new [project_name] --database=postgresql --skip-test-unit
creating database - rake db:create

start project: rails new [project_name] (without db)

start server: rails server -b 0.0.0.0
migrate information: rake db:migrate

rake db:rollback

create new model: rails g model

example: rails g model User first_name:string last_name:string email:string password:digest money:integer

rails g model Secret content:text user:references

example: rails g scaffold User first_name:string last_name:string email:string age:integer

rails generate controller <ControllerName>

example: rails g controller Users new index

test:

dojo$ bundle install
dojo$ rails generate rspec:install

Gemfile
group :development, :test do
  gem 'rspec-rails'
  gem 'factory_girl_rails'
  gem 'capybara'
end

       Prefix Verb   URI Pattern               Controller#Action
 likes_create GET    /likes/create(.:format)   likes#create
likes_destroy GET    /likes/destroy(.:format)  likes#destroy
         root GET    /                         users#new
secrets_index GET    /secrets/index(.:format)  secrets#index
     sessions POST   /sessions(.:format)       sessions#create
  new_session GET    /sessions/new(.:format)   sessions#new
      session DELETE /sessions/:id(.:format)   sessions#destroy
        users GET    /users(.:format)          users#index
              POST   /users(.:format)          users#create
     new_user GET    /users/new(.:format)      users#new
    edit_user GET    /users/:id/edit(.:format) users#edit
         user GET    /users/:id(.:format)      users#show
              PATCH  /users/:id(.:format)      users#update
              PUT    /users/:id(.:format)      users#update
              DELETE /users/:id(.:format)      users#destroy
      secrets GET    /secrets(.:format)        secrets#index
              POST   /secrets(.:format)        secrets#create
       secret DELETE /secrets/:id(.:format)    secrets#destroy
        likes POST   /likes(.:format)          likes#create
         like DELETE /likes/:id(.:format)      likes#destroy

postgresql: 
# access the postgres shell
vagrant@vagrant-ubuntu-trusty-64: ~$ sudo su postgres -c psql
# create a user
postgres=# CREATE ROLE vagrant SUPERUSER LOGIN;
# exiting the shell
postgres=# \q


rails new [project_name] --database=postgresql --skip-test-unit
creating database - rake db:create

# it will open psql in your development database
rails db
# you can run sql queries here
DojoSecrets_development=# \q # command to exit the shell