import { Renderer, ElementRef, OnInit, OnDestroy } from '@angular/core';
import { ScrollService } from './scroll.service';
import 'rxjs/add/operator/takeUntil';
export declare class AnimateOnScrollDirective implements OnInit, OnDestroy {
    private elementRef;
    private renderer;
    private scroll;
    private offsetTop;
    private isVisible;
    private winHeight;
    private ngUnsubscribe;
    animationName: string;
    constructor(elementRef: ElementRef, renderer: Renderer, scroll: ScrollService);
    ngOnInit(): void;
    ngOnDestroy(): void;
    /**
     * check for visibility of element in viewport to add animation
     *
     * @returns void
     */
    private manageVisibility();
    /**
     * utility function to mark element visible and add css class
     *
     * @returns void
     */
    private addAnimationClass();
    /**
     * utility function to add css class to element in DOM
     *
     * @param  {string} classname
     * @returns void
     */
    private setClass(classname);
    /**
     * get window height utility function
     *
     * @returns void
     */
    private getWinHeight();
    /**
     * get offsetTop value for element
     *
     * @returns void
     */
    private getOffsetTop();
}
